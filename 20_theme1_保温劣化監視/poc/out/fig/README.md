# 応募書類用 図版インベントリ

生成: `python3 poc/src/make_figures.py`（`poc/out/*.json` と `poc/data/*.tif` から再生成。
乱数シード固定・**同一入力なら出力はバイト単位で再現**することを確認済み）

依存: `numpy` `matplotlib` `matplotlib-fontja` `scipy` `tifffile` `pyproj`
（`tifffile`/`pyproj` が無い環境では図6のみスキップし、他は生成される）

---

## 提出版での採用（2026年8月8日確定）

様式4は**各設問に図表1点まで（図と表は同じ枠）**なので、提出物に載るのは3枚だけである。
提出物内の図番号はリポジトリの連番とは別に1から振り直してある。

| 提出物 | この階層のファイル | 置く設問 |
|---|---|---|
| 図1 | `fig0_overview.png` | ①提案の概要 |
| 図2 | `fig7_resolution_gap.png` | ③衛星データ活用の有効性 |
| 図3 | `fig8_operations_map.png` | ⑤実用化による社会発展性 |
| 表1 | （図版ではなく本文の表） | ④サービスの新規性 |

②課題の妥当性には図表を置かない。実施スケジュール欄の月別表は図表1点に数えない。

> **図の中に他図への参照やファイル名を書かないこと。** 提出物は図番号を1から振り直すため、
> 画像内に「（図5）」等が残っていると別の図を指してしまい、`poc/out/*.json` のような
> ファイル名は提出物の外を指す参照になる。2026年8月8日に fig0・fig4・fig8・fig9 の
> 画像内テキストから除去済み（`make_figures.py` を修正して再生成）。
> 図を作り直すときも同じ規律を守ること。

---

## 一次審査での使い方

一次審査は「技術的に本当に成立するのか」を短時間で判断される。図版の並びは
**図0 → 図8 → 図7 → 図9** を先頭に置くのが最も効く。この4枚で
「全体像」「運用時に何がどう見えるのか」「なぜ高分解能でなければ駄目か」
「実データで本当に動くのか」が揃う。残り（図1〜図6）は各節の裏付けとして本文中に配置する。

**KPIや誤差の数字だけでは伝わらない。図8・図9は「地図上で、どの資産が、どう判定
されたか」を直接見せる図であり、審査員が最初に知りたい「で、何が分かるの？」に
一枚で答える。** 図8が設備単位（HotSat-2相当・合成）、図9が複合体単位（実Landsat）で、
2枚は同じ「地図＋判定リスト」の型で揃えてあるため並べて読める。

| 図 | 主張（1行） | 根拠 | 提出版（12_提出版_様式4.md）での扱い |
|---|---|---|---|
| **fig0_overview** | 監視チェーンの全段が自主PoCで裏付けられており、できないことは自白済み | poc1〜poc4 の全結果 | **①に「図1」として採用** |
| **fig8_operations_map** | 地図上で、劣化=要点検／外装更新=点検不要／見逃し を資産ごとに提示 | poc4 モードBのシーンを再現し findings を重畳 | **⑤に「図3」として採用** |
| **fig7_resolution_gap** | 30m級TIRでは劣化信号が1/25に薄まる。3.5m級が要る理由 | poc2の合成シーンを3画素サイズで観測 | **③に「図2」として採用** |
| **fig9_realdata_map** | 同じ運用ビューを実Landsatに適用し、6複合体すべて健全判定＝誤検知ゼロ | poc4 モードA（実データ6エポック） | 不採用（⑤の代替案） |
| **fig6_realdata** | 実Landsatで前処理〜検知が通り、共通モード除去で振れ幅36.0K→7.7K、誤検知ゼロ | poc4 モードA（実データ6エポック） | 不採用（③の代替案） |
| fig1_snr_budget | 何が見えて何が見えないかをSNRで明示 | poc1 誤差バジェット | 不採用 |
| fig1b_method_budget | 絶対値比較は誤差RSS 9.4Kで物理的に不成立 | poc1 誤差バジェット | 不採用（②に置くと本文と接点が無いため見送り） |
| fig2_unit_auc | 兄弟差分の時間変化（二重差分）が推定器の本命 | poc2 合成シーンMC | 不採用 |
| fig3_rain_feature | 降雨後特徴量は現行1機では機会的。必須機能に置かない | poc3 実気象2020-2025 | 不採用 |
| fig4_pipeline | 劣化を検出し、外装更新を誤報せず、見逃しも開示する | poc4 合成14エポック | 不採用 |
| fig5_tank_stack | タンクパッチは時系列スタックで限界域から検出域へ | poc2 v2検知器 | 不採用 |

---

## 各図の注意点（審査員に突かれうる点）

- **fig6**: AOIは±数百m精度の概略であり現地検証が未了。図の副題と07・08の本文で
  必ずその旨を併記すること。Landsatは昼間パスかつ30m画素なので、これは
  「複合体レベルで手順が通る」ことの実証であって設備単位の実証ではない。
  設備単位はHotSat-2サンプル待ち（09_JSI照会文案.md）。
- **fig7**: 3.5m側はHotSat-2の実データではなく**合成シーンの同条件サンプリング**。
  「HotSat-2相当」という表記を外さないこと。NEdTは両者1K相当で揃えてあり、
  比較の主題は放射感度ではなく空間分解能である旨を本文に書く。
- **fig4右パネル**: TANK04は健全参照ではなく**パッチ注入資産（単発では未検出）**。
  凡例・注記を付録（07の注入真値表）と一致させてある。ここを「参照(健全)」に
  戻すと付録と矛盾するので注意（review_v2 指摘S-1）。
- **fig1**: 各バーのラベルにエポック数を明記してある（単発／二重差分×6エポック）。
  x軸ラベルを「単発観測」に戻すと図内で自己矛盾する（review_v2 指摘L-1）。
- **fig0**: 数値はすべて `poc/out/*.json` から実行時に読み込んでおり、
  PoCを再実行すれば図0の数値も自動追随する（手打ちの数値はない）。
- **fig8**: 地図は poc4 モードBのシーンを**再現**したもの。再現にあたっては
  `poc4_pipeline.py` が poc2ヘッダを exec した後に `rng` を **seed 123** へ貼り替えて
  いる点を必ず踏襲すること（poc2既定の seed 42 のままだと別の乱数シーンになり、
  地図上の資産配置と findings が食い違う）。`poc4_series.json` と完全一致することを
  確認済み。判定区分は action 文の「εアーティファクト」の語で分けている
  （「外装」で分けると UNIT00 の action にも同語が含まれるため誤判定になる）。
- **fig9**: 実データ側には既知の劣化事象がないため、示せるのは
  「健全なものを健全と判定できる＝誤報を出さない」ことまで。**検出能力の実証ではない**。
  図8と対で使い、検出側は合成、誤報抑制側は実データ、と役割を明示すること。
