# 次セッション用プロンプト（コピペ用）

以下をそのまま新しいセッションの最初のメッセージに貼る。

---

```
NEDO Challenge, Satellite Data 応募プロジェクトの続きです。まず CLAUDE.md を読んで全体を把握してください。

## 現在地

提出実体は `12_提出版_様式4.md`（様式4への転記用原稿）。5設問すべてが500字程度に収まり、
事務局視点レビュー（nedo-secretariat-reviewer）を14サイクル回して実装可能な指摘0件に到達済み。
**実装で閉じられる作業は残っていない。残りはすべてユーザー（阿部）の行動である。**

## 今回やること（どれが来るかは状況次第）

1. **事務局照会（04）の回答が返ってきた場合の反映**
   - 回答は `10_提出様式メモ.md` §9「手続の記録」に原文で保存する
   - 質問3（過去テーマ重複）・質問2（産業プラント横展開）が渋かった場合の削除順序は
     `04_事務局照会文案.md` の「送付時の注意」2 にある。**削除ではなく置換で書いてある**ので
     字句どおり適用してよい（削るだけだと文が破断する）
   - **質問4（「500字程度」に図表の文字が算入されるか）が「算入する」だった場合**は、
     `12_提出版_様式4.md` の「【提出時に削除】質問4（字数の数え方）の回答別の着地」節の
     6手順を順に適用する。各手順の到達字数は実測値が入っている
   - 反映後は `python3 count_chars.py 12_提出版_様式4.md` で測り直し、**設問の見出し注記と
     着地節の数値**を更新する（このファイルで字数を書いてあるのはこの2系統だけ）

2. **応募者区分が決まった場合の反映**
   - 12の「様式1・2へ転記する項目（ユーザー記入）」が転記の元データ
   - **個人応募者用は様式番号が違う**（提案書＝様式3、参加法人用の様式3は無い、応募者情報に
     居住地・国籍が加わる）。12の機械チェック冒頭に読み替え注記がある

3. **⑤の実用化年次の確認**（12の先行タスク6）
   - 現在は既定値「2027年度=協力先実証／2028年度=臨海1地区／2029年度以降=多拠点・海外」。
     事業計画と食い違えば書き換える。年次を落として順序だけにするのは審査項目を空にするので不可

4. **提出作業の伴走**
   - 12の文末チェックリスト（先行タスク1〜6＋機械チェック7〜27d）を上から消し込むだけで進む
   - 提出は8/28（金）までを推奨。8/31正午が締切

## 読む順序

1. CLAUDE.md — 全体像
2. `12_提出版_様式4.md` — **提出実体**。文末の3つの「【提出時に削除】」節（図表の割当／質問4の
   回答別の着地／転記対応表）とチェックリストに、判断の根拠が全部入っている
3. `review_v4_様式適合.md` 文末 — 14サイクル分のレビュー記録と学び（同じ失敗を繰り返さないため）
4. `koubo/` — 事務条項の最終根拠。10_提出様式メモ.md と食い違ったら原文が優先

## 踏んではいけない罠（レビュー14サイクルで実際に踏んだもの）

- **図の中の文字を検査対象から外さない**。fig0・fig8の画像内に `poc/out/*.json` などのファイル名と
  「（図5）」等の他図参照が焼き込まれていた。提出物では図番号を1から振り直しているので、
  画像内の「図2」が別物を指す衝突まで起きる。図を差し替えたら必ず画像として目視する
- **同じ数値を複数箇所に書き写さない**。4回再発した。字数は「設問の見出し注記」と
  「質問4の回答別の着地」節の2系統だけに集約してある。増やさないこと
- **見込みの数字を書かない**。削減手順の到達字数は、実際に適用して測ってから書く
- **フォールバック手順は削除指示ではなく置換指示で書く**。字句どおり適用して文が破断しないか確かめる
- **位置を件数・段落序数で指定しない**。箇所名と先頭句で指定する
- **提出物に未完了の時制を書かない**（「照会中」「打診予定」）。照会の事実は10 §9に残す

## やらないこと

- 内容の良し悪しの再検討（技術・事業性・新規性は review_審査員/技術/事業性/v2 で決着済み）
- 数値の変更（PoC結果は 07_PoC中間結果.md と poc/out/*.json が出典。変えるなら再実行が必要）
- ユーザー行動が必要な項目を勝手に埋めること（応募者情報・照会の送付・協力先打診・⑤の年次）
- 指摘を無理に作ること。実装で閉じられる不備が無ければ0件と書く

## Git

CLAUDE.md の「Git運用ルール」に従ってください（**PRは作成したら即マージ**、マージ後は購読も解除）。
```

---

## 補足（プロンプトには含めないが、必要になったら）

- **要項を取り直したいとき**: `git push -f origin <作業ブランチ>:trigger-fetch-koubo` で GitHub Actions が走り、数分後に `koubo-docs` ブランチが更新される。手順は `koubo/README.md`
- **PDFのテキスト化**: `pdftotext -layout`。未導入なら `apt-get update -qq && apt-get install -y -qq poppler-utils`（update を省くと404）。`pypdf`・`pdfminer.six` はこの環境では使えない
- **図版の再生成**: `pip install numpy matplotlib matplotlib-fontja scipy tifffile pyproj` の後 `python3 poc/src/make_figures.py`。所要約1分。同一入力なら出力はバイト単位で再現するので、変更した図だけが `git status` に出る（これが「意図せず他の図が変わっていない」ことの確認になる）
- **サイクルの回し方**: 1サイクルにつき「レビュー→指摘を反映→コミット・プッシュ」。エージェントには前サイクルで何を直したかを伝え、**主張を信用せず現物で確認するよう指示する**こと。指摘がゼロに近づいたら「無理に指摘を作らないでください。実装で閉じられる不備が無ければ0件と明記してください」と明示的に伝える
- **レビューの効き方の観察**: 本文が固まった後は、指摘は周辺ファイル（照会文案・様式メモ・引き継ぎ書）に残った古い前提へ移る。本体だけを見ていると気づけない
