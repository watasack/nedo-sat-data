# PoC中間結果レポート（シミュレーション版）

作成日: 2026年8月8日（同日更新: GitHub Actions経由で実データを取得し、PoC-3を実気象データ版、PoC-4を実Landsat版に差し替え済み） ／ 実行環境: クラウドサンドボックス

## 結論サマリ

> **【2026年8月11日】この節と PoC-1〜4 の数値には、第2次PoC（文末）で訂正が入ったものがある。
> 引用する前に文末の「第2次PoC」節を必ず読むこと。** 主な訂正は3点:
> **(1) NEdTは「全判定の」分水嶺ではない**（提案の骨格は依存しない。効くのはタンクパッチだけ）、
> **(2) タンクパッチの +3K/100m² 12エポック 0.88 は再現しない**（試行12回で0.73）、
> **(3) 実帯域3.7-4.95µmでの再計算は済み**（SNRは3〜7%低いだけ）。
> なお**提出物が引用している二重差分0.92は保守側で訂正不要**（試行40回で0.980。PoC-7）。

骨子v0.2の測定設計（シーン内相対比較・兄弟資産差分・二重差分・ステップ/トレンド分離・高ΔT資産への縮退線）は、**物理計算とモンテカルロの両方で定量的に支持された**。「タンク屋根の劣化パッチ検知」は単発観測では物理的に限界域（SNR<1、冷屋根でのNEdT増幅が原因）だが、**v2検知器（放射デトレンド＋時系列残差スタック）で月次1年分を蓄積すればAUC 0.88〜0.96に到達**することがシミュレーションで確認された。NEdT実力値の確認（JSI照会）が引き続き分水嶺。「降雨後回復速度」特徴量は、川崎の実気象データ（2020-2025）で再計算した結果、現行1機体制で**年4回程度（タスキング成功率0.6なら年2回弱）しか成立しない**ことが確認され、v0.2での「機会的補助特徴量への降格」が正しかったことを裏付けた（気候値パラメトリック版の年0〜1回より緩和されたが、「月次の劣化指紋にならない」結論は不変）。

---

## PoC-1: MWIR放射伝達 誤差バジェット（poc/src/poc1_error_budget.py）

前提: MWIR 3.4-4.2µm（帯域は仮定。HotSat-2の夜間帯は3.7〜4.95µm。**実帯域での再計算は PoC-5(a) で完了した**——ゲイン−8%・天空スイング悪化・低ε屋根のNEdT増幅は改善で、SNRは3〜7%低いだけ）、夜間、外気15℃、τ=0.75、風化アルミ外装 ε=0.25、センサNEdT=1K仮定（実力値は非公開・要JSI照会）

**感度ゲイン**: dTb/dTs = 0.53 — 表面の3K差は輝度温度では1.6Kにしか写らない（ε・τによる希釈）

**天空条件スイング**（晴天夜⇔曇天夜の見かけ温度変化、表面は不変）:
| 外装 | ε | 見かけ変動 |
|---|---|---|
| 新品アルミ | 0.10 | **+13.6K** |
| 風化アルミ | 0.25 | **+8.6K** |
| 経年・塗装劣化 | 0.40 | +5.6K |
| 塗装/錆/非金属 | 0.90 | +0.6K |

→ 技術レビューT-1の「5〜15K」主張を確認。**撮像間の絶対値比較は物理的に成立しない**（誤差RSS 9.4K vs 信号1.6K）

**測定方式別バジェットと検出判定（SNR）**:
| 検出対象 | 推定器 | SNR | 判定 |
|---|---|---|---|
| 加熱炉・高温配管系の保温不良(+15K) | シーン内コントラスト | **4.6** | 検出可能 |
| タンク屋根の劣化パッチ(+5K,100m²) | 同上(8px集約) | 1.6 | 限界域（NEdT実力値次第） |
| タンク屋根の劣化パッチ(+3K,100m²) | 同上 | 0.97 | 限界域 |
| ユニット面的劣化(+1K) 対双子(単発) | 兄弟差分（静的） | 0.19 | 単発では不可（資産間ε差が支配） |
| ユニット面的劣化(+1K) | **兄弟差分の時間変化（二重差分）×6エポック** | **1.9** | 限界域〜可能 |
| 個別配管CUIスポット(f=0.15) | — | 0.19 | 不可（設計通り対象外） |

→ 骨子2.2節の設計3原則と縮退線（高ΔT資産）は物理が支持。**静的な兄弟差分ではなく「兄弟差分の時間変化」が正しい推定器**であることが明確化（v0.2の記載と整合、応募書類ではこの語で統一推奨）

> **【2026年8月11日・PoC-5(b)】上の表の数値には単位の混在があった。**
> 実効NEdT を `NEdT/g`（表面温度の単位）で作り、輝度温度の単位の誤差項と同じRSSに
> 入れていた。また差動経年の項を `/√6` で割ってエポック平均で落ちる項として扱っていた。
> 輝度温度領域に統一し差動経年を落ちない項に移すと、**加熱炉は4.6でほぼ不変（縮退線の
> 主張は無傷）だが、二重差分6エポックは1.9 → 1.28、タンクパッチ+5Kは1.6 → 1.35**になる。
> **引用するときは PoC-5 の表を使うこと。**

## PoC-2: 合成シーン モンテカルロ（poc/src/poc2_scene_sim.py）

1km²合成プラント（タンク24基・ユニット8対）、PSF 3.5m、NEdT 1K、幾何ずれ±0.5px、天空・τの撮像間変動込み。

**ユニット面的劣化の判別AUC**（本PoCの最重要結果）:
| 劣化量 | 静的兄弟差分 | **二重差分（前後6エポック）** |
|---|---|---|
| +0.5K | 0.44（判別不能） | **0.86** |
| +1.0K | 0.54 | **0.92** |
| +2.0K | 0.69 | **0.997** |

**ステップ変化検知**（+2K、12エポック、z>3）: 検出率61%、**誤報率0%（0/30）**

**タンクパッチ検知（v2検知器で解決済み）**: 旧検知器（3px侵食・単発）はAUC 0.49〜0.65に留まっていたが、原因分析の結果、アルゴリズム課題に加えて**新たな物理知見**が判明: 低ε屋根は見かけTbが266〜280Kと低く、Planck逆変換の非線形性により**300K基準のNEdT 1Kが屋根上では約2〜4K（屋根中央値の典型で2.5〜2.7K）の輝度温度ノイズに増幅**される（増幅倍率はTb依存: 266Kで3.8倍、272Kで2.9倍、280Kで2.1倍〈帯域3.4-4.2µm〉）。このため+5K/100m²パッチ（希釈後Tbコントラスト≈2.3K）は単発観測では原理的にSNR<1であり、単発AUC 0.5〜0.65は検知器ではなく物理の限界。
**v2検知器**（放射方向デトレンド＝同心円アニュラス比較で縁勾配を除去＋ノイズ等価輝度正規化＋**月次時系列の残差スタック**＋PSF整合フィルタ、タンク径は実プラント準拠D40〜80m）による判別AUC:
| パッチ | 単発 | 6エポック | **12エポック(1年)** |
|---|---|---|---|
| +5K/100m² | 0.64 | 0.89 | **0.96** |
| +3K/100m² | 0.56 | 0.83 | **0.88** |
| +2K/100m² | 0.45 | 0.59 | 0.66 |

注: 上のAUCは可視領域内（1px侵食後マスク内）・持続性パッチ（評価期間の最初から存在）・（ユニット二重差分は）変化点既知の前後分割、という条件付きの値であり、実運用より上振れ方向の評価条件を含む。縁近傍の劣化は可視面カバレッジ仕様（原則3）の対象外として扱い、変化点未知の実務条件はステップ走査が対応する（検出率61%はPoC-2の評価値であり、パイプラインへの実装がPoC-4）。

→ **タンクパッチも「時系列でしか見えない」対象**であることが定量化された。単発スクリーニングではなく月次監視の蓄積で限界域から検出域へ移る、という骨子の時系列設計と完全に整合。
> **【2026年8月11日・PoC-5b】上のv2検知器のAUCは試行4回の値で、+3Kは再現しなかった。**
> 試行を12回に増やすと **+3K/100m² は 6エポック 0.83 → 0.615、12エポック 0.88 → 0.730
> [0.679, 0.777]**（+5K は 0.89 → 0.870、0.96 → 0.915 でほぼ再現）。
> AUCの標準誤差が0.06級なので4回では足りていなかった。**引用は PoC-5b の値を使うこと。**
> なお実帯域では改善する（+5K 12ep 0.946、+3K 12ep 0.793）。

**副産物の物理知見**: (1) 低ε屋根の「見かけの冷たさ」と縁勾配は実データでも必ず起きる。資産マスクの境界侵食設計はパイプラインの必須仕様（v0.2の位置合わせ・エロージョン記載を裏付け）。(2) 冷屋根でのNEdT増幅により、**NEdTの実力値だけでなく「低輝度シーンでのノイズ特性（基準温度）」がJSI照会の必須項目**になる。

## PoC-3: 降雨後特徴量の成立確率（poc/src/poc3_rain_feature.py）

**【実データ版】** 川崎の実気象データ（Open-Meteo時別、2020-2025、降雨イベント実測62回/年）によるモンテカルロ。成立条件: 降雨前7日以内のベースライン+降雨後96h以内の晴天夜間撮像（単点=1回、回復速度=2回）。晴天判定は**衛星パス時刻（22時JST想定）の雲量<30%**（夜間ウィンドウ最小雲量を使うと約2.4倍過大評価になるため不採用）。

| 体制 | 単点特徴量 成立/年 | **回復速度（傾き） 成立/年** |
|---|---|---|
| 現行1機（2日おき夜側パス） | 21 | **4** |
| 現行1機（3日・タスキング競合） | 12 | **1.5** |
| 3機化 | 37 | 22 |
| 9機化 | 39 | 24 |

感度分析（1機・2日おき）: パス時刻を深夜1時にしても傾き年5回程度で不変。タスキング成功率0.6なら**年1.8回**。楽観上限（夜間5時間のどこかで晴れ）でも年10回。

→ **「回復速度」特徴量は現行体制では年数回の機会的成立に留まり**、技術レビューT-3とv0.2の降格判断を実気象データで確認（気候値パラメトリック版の年0〜1回より緩和されたのは、関東の冬の晴天率が気候値近似より高いため）。単点版（降雨後の高温残留=滞留水疑い）は年12〜21回で、機会的特徴量として実用に足る頻度。3機化以降で傾き特徴量は年20回超となり定常特徴量へ昇格可能（骨子のロードマップ記述と整合）。

## PoC-4: パイプライン通しリハーサル（poc/src/poc4_pipeline.py）

14エポック合成時系列でingest→位置合わせ→マスク集約→共通モード正規化→兄弟差分→ステップ走査→優先順位レポート（CSV）まで通し実行。3つの注入イベントに対し:

| 注入イベント | 結果 |
|---|---|
| ユニット0: ep08で+2K面的劣化 | **検出**（z=4.7、変化点をep08と正しく同定、「点検派遣」アクション） |
| タンク10: ep10で外装張替え（εアーティファクト） | **検出+正しく分類**（z=-11.4、「CMMS外装履歴の突合」アクション — 劣化と誤報しない） |
| タンク4: ep06以降の漸増小パッチ(+1.2K/60m²) | 未検出（PoC-1の限界域予測と整合） |
| 誤報 | **0件**（健全29資産） |

**【実データ版も実行済み】** Landsat C2L2 ST_B10（30m、2023-2026の四半期14シーン、京浜切り出し）で同パイプラインを通し実行:
- 京浜はLandsatの隣接パス境界に位置し、8/14シーンはスワス端欠測 → 参照面有効性で自動フィルタし6エポックを採用（実運用でも起きる品質管理の実演）。**【2026年8月11日・PoC-6(a)】これは参照面の置き場所の失敗だった**——欠測しているのは市街地参照AOIの側で、扇島製鉄所・大黒町火力は14シーン全部有効。**参照面を全シーン共通の有効域に置けば6→14エポックになる**。**2026年8月11日に実装済み**——`poc4_pipeline.py` は参照面を2種（新＝鶴見区市街地・全14シーン有効／旧＝川崎市街地）で併走し両方を `variants` に保存する。**旧参照面の結果は 36.02K→7.65K を正確に再現するので提出物の数値は据え置きでよい**。ただし**改善は×1.53ではなく約1.2倍**（review_v7 P-19）
- 複合体レベルの概略AOI 6箇所（川崎火力・東扇島火力・浮島/水江町製油所地区・扇島製鉄所地区・大黒町火力地区）+市街地参照面で、共通モード正規化→兄弟差分（火力対・製油所対）→ステップ走査まで完走
- 結果: 既知の大規模イベントがない期間で**全AOI・全ペアが「有意な段差なし」（明白な偽陽性0。ただし有効6エポック・実測|z|≤1.1のため検出力は限定的で、意味するのは「パイプラインが実データで完走し明白な偽陽性を出さない」ことの確認まで）**。季節共通モードは市街地参照差で大幅に低減（ただし海陸風・水面隣接の局所季節成分が数K残存する〈例: 東扇島 +1.3〜-5.7K〉。実運用では晴天夜間層別と季節モデルで扱う）
- **限界の実証**: 30m画素では複合体単位の集約が限界で、タンク・ユニット単位の解析は不可能——「設備単位はHotSat-2でしか解けない」という本提案の前提を実データで裏付けた（結果: poc4_results_real.json）

---

# 第2次PoC（2026年8月11日追加） ── 仮定・単位・率の点検

PoC-1〜4 には「未回答の外部要因に結論がぶら下がっている」「仮定が検証されていない」
「数字が率になっていない」の3種類の弱点が残っていた。PoC-5・5b・6・7 でそこを埋めた。
**指摘の一覧と物差しは `review_v7_PoC数値.md`（P-1〜P-18）。**

**結論から先に書く。「NEdTが全判定の分水嶺」は正しくなかった。対象によって分水嶺が違う。**

| 対象 | 分水嶺 | 根拠 |
|---|---|---|
| 加熱炉・高温配管の縮退線 | **どちらでもない**（NEdT 3Kでも単発で検出可能） | PoC-5(e) |
| ユニット面的劣化の二重差分 | **兄弟資産間の放射率の差動経年**。NEdTは上限を動かさず、決めるのはエポック数だけ | PoC-5(e)(f) |
| タンク屋根の劣化パッチ | **NEdT**。位置未知の整合フィルタ最大値なので雑音に非線形に劣化する | PoC-5b(a) |

**提案の骨格（縮退線＋ユニット二重差分）はNEdTに依存しない。**
JSI照会が決めるのは「タンクパッチを商品に入れられるか」である。
したがって**回答が8/31に間に合わなくても提案の成否は動かない**。

一方、**ユニット二重差分の上限を決めている兄弟資産間の放射率の差動経年は、
PoC-1 の仮定値0.3Kで、出典が無く、実測もされていない**。
**Phase 0 にはこの実測を足すべきである。** 同一構内の同型ユニット対とCMMSの外装履歴が
あれば自前で測れるので、外部の回答待ちにならない。

**効きの大きさは PoC-7 でモンテカルロに実装して測った**（+1K・二重差分AUC、160対160）:

| 差動経年 | 0.0 K | 0.15 K | 0.3 K | 0.5 K |
|---|---|---|---|---|
| AUC | 0.980 | **0.921** | **0.788** | 0.697 |

**PoC-5 の解析（0.3Kで上限0.873）は楽観だった**——MCの低下幅は解析予測の約2倍である。
上限AUCを引用する場面ではMC側（PoC-7）の値を使うこと。
なお **0.15K のときの 0.921 は提出物が引用している 0.92 とほぼ一致する。**

## PoC-5: 実帯域での再計算とNEdT掃引（poc/src/poc5_band_nedt.py）

### (a) 実帯域 3.7-4.95µm での再計算 ── Phase 0 の残タスクを閉じた

PoC-1 の帯域 3.4-4.2µm は仮定値だった。HotSat-2 の夜間帯 3.7-4.95µm で引き直した:

| 量 | 仮定帯域 3.4-4.2µm | **実帯域 3.7-4.95µm** |
|---|---|---|
| 感度ゲイン dTb/dTs | 0.528 | 0.484（−8%） |
| 天空スイング（晴⇔曇） | 8.57 K | **10.17 K（悪化）** |
| 低ε屋根の見かけTb | 276.1 K | 274.4 K |
| NEdT増幅（266K / 272K / 280K） | 3.81 / 2.93 / 2.11 倍 | **3.11 / 2.49 / 1.88 倍（改善）** |

**結論: 帯域の仮定は結論を変えていなかった。** ゲインと天空スイングは実帯域でやや不利、
低ε屋根でのPlanck非線形増幅はやや有利で、SNRは実帯域で3〜7%低いだけである。
07 の「実帯域での再計算はPhase 0」は**これで閉じた**。

### (b) PoC-1 に単位の混在があった

PoC-1 の (F) 節は SNR のRSSに、**輝度温度の単位の項**（ε不確かさ0.76K、PSF混合0.8K）と
**表面温度の単位の項**（実効NEdT = NEdT/g）を混ぜて入れていた。g≈0.5 なので
NEdT項を約2倍に見せる方向に働く。PoC-5 は全部**輝度温度領域**に統一した
（NEdT項は `NEdT_ref × amp(見かけTb)`。これは PoC-2 が見つけた増幅そのもの）。

もう1点、PoC-1 は差動経年の項も `/√6` で割っており、**エポック平均で落ちる項として
扱っていた**。放射率の差動経年は時間とともに開く量なので落ちない。分けて扱った。

この2つの修正で、SNRは公表値より下がる:

| ケース | PoC-1 公表値 | **PoC-5（実帯域・単位統一）** |
|---|---|---|
| 加熱炉・高温配管の保温不良(+15K) | 4.6 | 4.61 |
| タンク屋根パッチ(+5K, 8px集約) | 1.6 | 1.35 |
| ユニット面的劣化(+1K) 静的兄弟差分 | 0.19 | 0.24 |
| ユニット面的劣化(+1K) 二重差分6ep | 1.9 | **1.28** |

加熱炉ケースは一致する（ここが「検出可能」の唯一の行なので、**縮退線の主張は無傷**）。
下がったのは二重差分で、原因は差動経年を落ちない項に移したことである。

### (c) SNR→AUC の解析写像は使えるが、対象を選ぶ

等分散ガウス2群なら AUC = Φ(SNR/√2)。PoC-2 のモンテカルロと突き合わせた。
**前提を揃える**（PoC-2 は差動経年を模擬していないので0にする）と:

| 劣化量 | PoC-2 のMC | 前提を揃えた解析AUC | 差 |
|---|---|---|---|
| +0.5K | 0.86 | 0.78 | −0.08 |
| +1.0K | 0.92 | 0.939 | **+0.02** |
| +2.0K | 0.997 | 0.999 | **+0.00** |

**+1K・+2K では写像が使える。** ただし**タンクパッチには使えない**——
パッチ位置が未知でPSF整合フィルタの最大値を取る＝多重比較なので、
解析写像は単発で +0.20 も過大に出る（MC 0.64 対 解析 0.838）。
だからタンクパッチのNEdT依存は PoC-5b で検知器そのものを回した。

### (d) PoC-2 のMCが模擬していない項

前提を揃える作業で分かったことがある。
**PoC-2 の合成シーンは兄弟資産間の放射率の差動経年を模擬していない**
（資産ごとのεを静的に振ってあるだけで、時間とともに兄弟間の差が開く成分が無い）。
この項を入れると **+1K・6エポックのAUCは 0.92 → 0.83** になる。

`08`/`12` が引用している「二重差分でAUC 0.86〜0.997（シミュレーション）」は
**この項を含まない値**である。**数字を下げる必要はない**が、
「同一設計・同一施工年・同一外装履歴の兄弟資産を選ぶ」ことが**AUCの前提条件**である
ことは明示すべきである。これは提案の設計原則としてすでに書いてあるので、
文言の追加ではなく**前提として読ませる**問題である。

### (e) NEdT掃引 ── 回答が来なくても結論が言える

NEdT 0.5〜3K（公表値は「<2K」。3Kは外れた場合の備え）で振った結果:

**上限AUC（エポックを無限に積んだとき）は NEdT に一切依存しない。**

| ケース | NEdT=0.5K | 1.0K | 1.5K | 2.0K | 3.0K |
|---|---|---|---|---|---|
| 加熱炉(+15K) | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 |
| ユニット(+1K) 二重差分 | 0.873 | 0.873 | 0.873 | 0.873 | 0.873 |
| ユニット(+0.5K) 二重差分 | 0.716 | 0.716 | 0.716 | 0.716 | 0.716 |

NEdT が決めるのは**到達に要るエポック数**だけである（AUC0.90到達に要るエポック数、実帯域）:

| ケース | 0.5K | 1.0K | 1.5K | 2.0K | 3.0K |
|---|---|---|---|---|---|
| 加熱炉(+15K) | 1 | 1 | 1 | 1 | 1 |
| タンクパッチ(+5K)※ | 2 | 8 | 17 | 30 | 68 |

※タンクパッチのAUCは解析写像が過大なので、実際の値は PoC-5b を使う。

**したがって「JSIの回答が8/31に間に合わなければ何も書き足さない」という方針のままでも、
提案の成否は動かない。** 回答が決めるのは月次監視で何年目に精度が出るかである。

### (f) 上限を決めているのは差動経年

| 差動経年 | ユニット(+1K)の上限AUC | ユニット(+0.5K)の上限AUC |
|---|---|---|
| 0.15 K | 0.989 | 0.873 |
| 0.20 K | 0.957 | 0.804 |
| **0.30 K（PoC-1の仮定値）** | **0.873** | **0.716** |
| 0.50 K | 0.753 | 0.634 |

**この値は PoC-1 の仮定値で、出典が無く、PoC-2 も模擬しておらず、実測もされていない。**
上限AUCが 0.99 か 0.75 かがこの1つの数字で決まる。

**ただしこの上限は悪い側の上界である。** ここでは差動経年をエポック平均で落ちない
一定バイアスとして扱ったが、実際には緩やかな**トレンド**なので、ステップ走査が
段差とトレンドを分離できる分だけ実害は小さくなる（PoC-4 は `slope_per_ep` を出している）。
到達不能の宣言ではない。

**測り方**: 同一構内の同型ユニット対を、外装張替え履歴が既知の期間で追う。
CMMSの外装履歴と突き合わせれば「張替え直後＝差動経年ゼロ」の起点が取れる。

## PoC-5b: タンクパッチ検知器v2をNEdTと帯域を振って回す（poc/src/poc5b_tank_nedt_mc.py）

解析写像がタンクパッチで破れるので、**検知器そのものを回した**。
試行12回（PoC-2 は4回）、24エポック生成、AUCにブートストラップ90%区間を付けた。
評価条件は PoC-2 と同一の楽観条件（可視面内・持続パッチ・侵食後マスク内）なので直接比較できる。

### (a) NEdT はタンクパッチには効く ── PoC-5 の結論に限定が付く

PoC-5 は「NEdT は上限AUCを動かさない」と出したが、それは**兄弟差分の時間変化（二重差分）
＝場所が既知の1検定**についての話である。タンクパッチ検知は位置が未知で整合フィルタの
最大値を取るので、**雑音に対して非線形に劣化する**。実測（実帯域）:

| NEdT | +5K/100m² 12ep | 24ep | AUC0.90到達 | +3K/100m² 12ep | 24ep | AUC0.90到達 |
|---|---|---|---|---|---|---|
| 0.5 K | 0.961 [0.94, 0.98] | 0.971 | **3ep** | 0.871 [0.83, 0.90] | 0.909 | 18ep |
| 1.0 K | 0.946 [0.92, 0.97] | 0.963 | **12ep** | 0.793 [0.75, 0.84] | 0.877 | >24ep |
| 2.0 K | 0.760 [0.71, 0.81] | 0.853 | **>24ep** | 0.563 [0.51, 0.62] | 0.630 | >24ep |
| 3.0 K | 0.582 [0.53, 0.64] | 0.674 | >24ep | 0.543 [0.49, 0.60] | 0.561 | >24ep |

**したがって「NEdTは分水嶺ではない」は次のように限定する必要がある**:

- **加熱炉・高温配管の縮退線とユニット面的劣化の二重差分**については、NEdTは上限を
  動かさない（決めるのはエポック数だけ）。**提案の骨格はNEdTに依存しない**
- **タンクパッチについてはNEdTが効く**。2Kなら +5K/100m² が2年（24エポック）でも
  AUC 0.85 に留まり、+3K/100m² は0.63で使えない
- つまり **JSI照会が決めるのは「タンクパッチを商品に入れられるか」であって、
  提案の骨格の成否ではない**

### (b) 実帯域はタンクパッチには有利

| パッチ | 1ep | 6ep | 12ep | 24ep |
|---|---|---|---|---|
| +5K/100m²（仮定帯域→実帯域） | 0.555→0.629 | 0.870→0.878 | 0.915→**0.946** | 0.953→0.963 |
| +3K/100m²（同） | 0.516→0.527 | 0.615→0.694 | 0.730→**0.793** | 0.842→0.877 |

低ε屋根でのNEdT増幅が実帯域で小さい（266Kで3.11倍 対 3.81倍）ためである。
**実帯域への差し替えはタンクパッチについては有利に働く。**

### (c) PoC-2 の +3K の公表値は再現しなかった

| パッチ | エポック | PoC-2 公表値（試行4回） | **PoC-5b（試行12回・仮定帯域・NEdT1K）** | 差 |
|---|---|---|---|---|
| +5K/100m² | 6ep | 0.89 | 0.870 | −0.02 |
| +5K/100m² | 12ep | 0.96 | 0.915 [0.884, 0.942] | −0.045 |
| +3K/100m² | 6ep | 0.83 | **0.615** | **−0.215** |
| +3K/100m² | 12ep | 0.88 | **0.730 [0.679, 0.777]** | **−0.15** |

**+5K は再現する（1標準誤差程度）が、+3K は再現しない。** 公表値0.88は
本PoCの90%区間 [0.68, 0.78] の外にある。原因は**試行4回では標本が足りていなかった**
こと（AUCの標準誤差が0.06級。PoC-2 の +3K は運の良い引きだったと考えられる）。

**提出物への影響は無い。** `12_提出版_様式4.md` が引用しているAUCは
**ユニット面的劣化の二重差分0.92と静的比較0.54だけ**で、タンクパッチの値は入っていない。
07 の反映ポイント5だけを直した（下）。

**教訓**: AUCはエポック数に対し単調増加するはずなので、**逆転があれば標本不足の印**である。
PoC-5b は逆転回数をJSONに記録するようにした。試行4回では逆転が多発し、12回でほぼ消えた。

## PoC-7: 提出物の見出し数値 AUC 0.92 を区間つきで測り直す（poc/src/poc7_unit_dd_ci.py）

`12_提出版_様式4.md` 設問④が引用しているPoC由来の判別性能は
**「ユニット面的劣化+1Kの判別AUCは静的比較0.54に対し二重差分0.92」の1行だけ**である。
ところが PoC-2 は `N_TRIALS=6`・ユニット対8組中4組劣化なので、
**この0.92は陽性24個・陰性24個から出ており標準誤差0.05級**だった。
試行40回（160対160）＋ブートストラップ90%区間で測り直した。所要46分。

### 公表値は保守側だった ── 訂正不要

| 劣化量 | 公表値（24対24） | **PoC-7（160対160）** | 90%区間 |
|---|---|---|---|
| +0.5K | 0.86 | 0.785 | [0.744, 0.825] |
| **+1.0K** | **0.92** | **0.980** | **[0.970, 0.989]** |
| +2.0K | 0.997 | 1.000 | [1.000, 1.000] |

3つとも区間の外だが**ずれの向きが揃っていない**（+0.5Kは公表が高く、+1.0Kは低い）。
大きさは公表値の標準誤差の1〜1.3倍に収まる。**公表値は「間違い」ではなく「精度が足りない」。**

**提出物が引用している 0.92 は、より良い測定 0.980 に対して保守側である。訂正は要らない。**

### 差動経年の効きは解析予測の約2倍

PoC-2 が模擬していなかった兄弟間の放射率の差動経年を、
**後期の観測前に各部材のラック画素のεを独立に摂動する形でシミュレータに実装した**
（δε=(目標K/√2)×0.05/1.9。0.05→1.9K は PoC-1 (B) の実測）。上の表のとおり
+1K は 0.980 → 0.788（0.3K）で、**PoC-5 の解析予測（0.939→0.834、低下0.105）に対し
MCの低下は0.192＝約2倍**。解析はεドリフトを一定バイアスとして二乗和に足すが、
MCではεの摂動が見かけ温度に非線形に効き、トリム平均集約の応答も変わる。

**帯域は二重差分にはほとんど効かない**（仮定帯域0.980 → 実帯域0.977）。
タンクパッチでは実帯域が有利だった（PoC-5b）のと対照的で、
低ε屋根のNEdT増幅が効くかどうかの違いである。

## PoC-6: 実Landsatで雑音床と誤報率を測る（poc/src/poc6_real_noise_floor.py）

京浜の実Landsat ST_B10 14シーン（2023-2026）。150mブロックを**代用資産 11,422個**として
帰無母集団にした。代用資産に本物の変化が混ざる可能性は排除できないので、
**出る誤報率は上限**である。

### (a) PoC-4 が8シーンを捨てていたのは参照面の置き場所の失敗だった

PoC-4 は「京浜がLandsatの隣接パス境界にあり、スワス端で北西側が欠測する」として
14シーンのうち8シーンを捨て、6エポックで走っていた。実測すると:

- 8シーンでも**全景の53〜58%は有効**
- 欠測しているのは**市街地参照AOI（川崎市街地）の側**
- **扇島製鉄所・大黒町火力は14シーン全部で有効**、東扇島火力は13/14

**参照面を全シーン共通の有効域の内側に取れば、6エポックが14エポックになる**（除外0）。
**2026年8月11日に `poc4_pipeline.py` へ実装した**（新参照面＝鶴見区市街地
(139.6615, 35.4952, 139.6815, 35.5098)。旧参照面との相関 r=0.9998・差のσ0.28K）。

**ただし改善は 1/√n が示す ×1.53 ではない。** エポックを増やすと季節の幅も広がるので
資産別σが **1.71K（6エポック）→ 2.47K（14エポック）** に増える。実測の最小検知段差は
**中央値 4.5K → 3.7K ＝ 約1.2倍**で、6資産のうち大黒町火力は逆に悪化する（3.3K→4.5K）。
**当初この節に書いた「×1.53、6.8K→4.5K」は σ が同じままと仮定した過大な値だった**（review_v7 P-19）。

### (b) 雑音床 ── 「季節モデルで扱う」を数字にした

PoC-4 は残差に数K残る局所季節成分を「実運用では晴天夜間層別と季節モデルで扱う」と
将来形で書いていた。実測:

| 段階 | 資産時系列σの中央値 | p90 |
|---|---|---|
| 生（資産平均のみ除去） | 8.44 K | 13.53 K |
| シーン共通モード除去（PoC-4の市街地参照差に相当） | 3.92 K | 5.66 K |
| **ランク1季節モデル（資産ごとの季節感受度）** | **2.78 K** | 4.76 K |
| ランク2 | 1.33 K | 2.36 K |

**シーン一律の共通モード除去では足りず、資産ごとの季節感受度まで入れて初めて落ちる**
（ランク1成分が分散の90%を説明する）。東扇島の −5.7K はこの感受度の違いである。

### (c) 兄弟差分の雑音は離隔距離で増える ── 兄弟資産の選び方が決まる

| 離隔距離 | 兄弟差分σの中央値 | p90 |
|---|---|---|
| 150-300 m | **0.43 K** | 1.80 K |
| 300-600 m | 0.78 K | 3.34 K |
| 600-1200 m | 1.18 K | 4.96 K |
| 1200-2400 m | **1.79 K** | 5.69 K |
| 2400-5000 m | 2.63 K | 7.18 K |
| 5000-10000 m | 3.89 K | 8.68 K |

共通モードの相殺は「同じ微気象を共有している」ことに依存している。
**兄弟資産は同一設計であるだけでなく近接している必要がある。**
同一構内の同型ユニット（PoC-1/2の設定）はこの条件を満たすが、
**PoC-4 の実データ版が使った複合体間の対（川崎火力−東扇島火力で約2km、
浮島−水江町で約4km）は満たしていない**。実測|z|≤1.1 で何も出なかったのは当然である。

### (d) PoC-1/5 の二重差分バジェットを実データで突き合わせた

PoC-5 は二重差分の非NEdT雑音を「負荷残差の差動0.3K + 放射率の差動経年0.3K」の
RSS **0.42K** と置いている（いずれも仮定値）。実データで測れる対応量は
近接兄弟対（150-300m）の差分の時系列σ ＝ **0.43K**。**比 1.01。**

**限定**: Landsatは30m・昼間・TIR、HotSat-2は3.5m・夜間・MWIR。画素サイズ・時刻・波長が
すべて違うので、これは**桁の確認**であってバジェットの検証ではない。
夜間MWIRでの実測はHotSat-2サンプル入手後（09の回答待ち）。

### (e) 「偽陽性0件」を率に直した

ステップ走査 max|z| の実測分布（時間順をシャッフルした対照と比較）:

| 閾値 | 実データの超過率 | シャッフル対照 | **1プラント年の誤報件数** |
|---|---|---|---|
| z≥2.0 | 23.75% | 35.31% | 6.11 件 |
| z≥2.5 | 10.45% | 17.82% | 2.69 件 |
| **z≥3.0（PoC-4が使った値）** | **1.26%** | 8.60% | **0.32 件** |
| z≥4.0 | 0.11% | 2.17% | 0.03 件 |

換算は「超過率 × 30資産 × 12エポック/年 ÷ 14」。
**実データの裾はシャッフル対照より軽い**（季節構造がプールされた分散を膨らませて
z を抑える方向に働く）。年1件に抑える閾値は **z=2.80** で、
**PoC-4 が使っていた z=3 は 0.32件/プラント年に相当する。実用に足る。**

### (f) 検出力の限界（30m・複合体レベル）

最小検知段差（雑音床2.78K・検出率50%・年1件の閾値z=2.80）:

| エポック数 | 6 | 12 | 14 | 24 | 36 |
|---|---|---|---|---|---|
| 最小検知段差 | 6.36 K | 4.50 K | 4.17 K | 3.18 K | 2.60 K |

**6エポック・z=3 では 6.8 K が検出限界であり、提案が狙う1〜2Kの面的劣化は
6エポックでは原理的に検出できない。** PoC-4 の実データ版の「偽陽性0件」は
誤報耐性の証拠にはなるが検知能力の証拠にはならない——という PoC-4 の自白は正しく、
本PoCはその限界に数字を付けた。

**この数字を HotSat-2 の性能として読んではいけない。** 30m画素・複合体レベルの値である。
設備単位（3.5m級）では画素数と充填率が変わる。

---

## 応募書類への反映ポイント

1. 「兄弟差分の時間変化（二重差分）でAUC 0.86〜0.997（シミュレーション）」は応募書類に書ける実績数値。「シミュレーションによる」の限定を必ず付す（AUCは「1.00」と丸めない——完璧な判別は懐疑を招く）。**【2026年8月11日追記・PoC-5(d)】この値は兄弟資産間の放射率の差動経年を含まない**（PoC-2 の合成シーンが模擬していない）。入れると +1K・6エポックは 0.92 → 0.83。**提出物は「合成プラントのモンテカルロで」と明記しているので訂正は不要**だが、下の8の前提条件を意識して語ること
2. εアーティファクト（外装張替え）を検知して劣化と区別するデモは、審査での想定問答（T-7）への直接の回答になる
3. ~~NEdT実力値の照会（JSI）が全判定の分水嶺であることは変わらない — Phase 0タスクの最優先。~~ **【2026年8月11日改訂・PoC-5/5b】NEdTは「全判定の」分水嶺ではない。** **提案の骨格（加熱炉・高温配管の縮退線＋ユニット面的劣化の二重差分）はNEdTに依存しない**（上限AUCが一切動かず、決まるのは到達に要るエポック数だけ）。**NEdTが効くのはタンクパッチだけ**（位置未知の整合フィルタ最大値なので雑音に非線形に劣化する。2Kなら+5K/100m²が2年でもAUC 0.85止まり）。照会は引き続き送るが、**回答が8/31に間に合わなくても提案の骨格の成否は動かない**——決まるのは「タンクパッチを商品に入れられるか」である。**照会には「低輝度（260〜280K）シーンでのノイズ特性」を必ず含める**（冷屋根でのNEdT増幅の知見による）
4. 降雨後特徴量は「単点・機会的（実気象データで年12〜21回）」として記載し、「回復速度」（同・年数回）を約束しない。3機化で傾き年20回超→定常特徴量へ昇格、はロードマップに書ける
5. ~~タンクパッチは「単発では限界域、月次時系列の蓄積（v2検知器）で1年後にAUC 0.88〜0.96（シミュレーション）」として記載できる。~~ **【2026年8月11日改訂・PoC-5b】試行を4回から12回に増やすと +3K/100m² の公表値は再現しなかった**（12エポック 0.88 → **0.730 [0.679, 0.777]**）。**引用できる範囲は「実帯域・NEdT 1K・12エポックで +5K/100m² が AUC 0.95、+3K/100m² が 0.79」**（いずれもシミュレーション、90%区間つき）。**提出物への影響は無い**——`12_提出版_様式4.md` が引用しているAUCはユニット二重差分0.92と静的比較0.54だけで、タンクパッチの値は入っていない。「即時検知」を約束せず「監視の継続で精度が上がる」設計として語る方針は変えない
6. 図版は `poc/out/fig/` に生成済み（fig1: SNRバジェット、fig1b: 方式別誤差、fig2: 二重差分AUC、fig3: 降雨特徴量、fig4: パイプライン時系列、fig5: タンク時系列スタック）

### 第2次PoCからの追加（2026年8月11日）

7. **Phase 0 の最優先は「兄弟資産間の放射率の差動経年の実測」である**（PoC-5(f)）。この1つの値で上限AUCが0.99か0.75かが決まるのに、出典が無く実測もされていない。**同一構内の同型ユニット対＋CMMSの外装張替え履歴があれば自前で測れる**ので、外部の回答待ちにならない。**06の協力先打診で頼むべき材料はこれである**（14_外部連携の順序.md の目的B＝実証データの取得に属する）
8. **AUC 0.86〜0.997 を引用するときは「同一設計・同一施工年・同一外装履歴の兄弟資産を選ぶ」ことが前提条件であることを明示する**（PoC-5(d)）。PoC-2 の合成シーンは差動経年を模擬していないので、この前提が崩れると +1K・6エポックのAUCは 0.92 → 0.83 になる。**数字を下げる必要はないが、前提として読ませる必要がある**。提案の設計原則にはすでに書いてあるので文言追加ではない
9. **兄弟資産は「同一設計」だけでなく「近接」が条件である**（PoC-6(c)、実Landsatで実測）。兄弟差分の雑音は離隔150-300mで0.43K、2.4-5kmで2.63K。**同一構内の同型ユニットという設定はこの条件を満たしている**ので提案の設計は正しいが、**複合体をまたぐ兄弟対は成立しない**ことを知っておく
10. **「誤報0件」ではなく「z=3で0.32件/プラント年（実Landsatの代用資産11,422個で実測、上限値）」と書ける**（PoC-6(e)）。率になっているので運用側の質問に直接答える。**ただし30m・複合体レベルの値であって設備単位の性能ではない**という限定を必ず付ける
11. **実帯域3.7-4.95µmでの再計算は済んだ。SNRは仮定帯域より3〜7%低いだけ**（PoC-5(a)）。「帯域は仮定」という留保は外せる

## 実データ版への差し替え（完了）

GitHub Actionsランナー（`.github/workflows/fetch-poc-data.yml`、trigger-fetch-dataブランチへのpushで起動）で取得し、`poc/data/` に配置済み（poc-dataブランチにも保存）。poc3はモードA（実気象）、poc4はモードA（実Landsat・複合体レベルAOI）で再実行済み。残る実データ課題: (1) AOI座標の現地検証（現状±数百mの概略）、(2) HotSat-2サンプル入手後の設備単位デモ（JSI照会=09の回答待ち）。
